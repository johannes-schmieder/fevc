# Adjudication: KSS-BC-API12-FINAL-M

## Review metadata

- Packet: `reviews/gpt-pro/requests/KSS-BC-API12-FINAL-M/PACKET.zip`
- Response: `reviews/gpt-pro/responses/KSS-BC-API12-FINAL-M.md`
- Packet SHA-256: `ceb7fffed0a92bcdf9f9e7887fb0613fdef06a6f40d07025e48c671c25e1f47c`
- Verbatim response-body SHA-256: `e48b25055c21d59d529c6d4f4a1453136af4ddf5007671012219df5dd1473072`
- Adjudication date: 2026-08-14

## Disposition table

| Finding | Disposition | Repair or evidence |
|---|---|---|
| Exact and auto-to-exact sorted raw controls and encoded IDs before selecting the first safely eligible anchor. | accepted, reproduced, and repaired | Native API12 replay of the reviewer's eight-row determinant-four witness accepted both bases and returned gaps near `1.4e-9`. API13 gives every controlled exact/JLA/auto path one outcome/per-copy-target semantic order that excludes raw controls and IDs and withholds nonexchangeable ties. |
| The original witness remained sufficiently ill-conditioned that a common canonical matrix could still be amplified downstream. | independently diagnosed and repaired conservatively | API13 propagates the canonical-basis envelope through full-design and deletion conditioning. The original `d=.002` representations all return `AMBIGUOUS_CONTROL_BASIS`; the better-conditioned `d=.2` family is accepted and agrees under the control map and simultaneous worker/firm/match relabeling across both deletion and nuisance modes and exact/auto dispatch. |
| API12's fixed anchor-error proxy was not a demonstrated dimension-aware forward bound. | accepted and repaired | API13 caps controls at 32, uses `gamma_m` Gram/Cholesky/product terms, converts maximum-column inverse residuals with `sqrt(k)`, requires positive perturbation denominators, includes score/cutoff and projector/anchor/span errors, and applies the stated `E/(r-E) <= 1e-8` downstream gates. |
| Match and target JLA could allocate `sum(frequency)` signs without `physical_limit()`. | accepted and repaired | The retained exact physical total is checked once for every selected JLA route before any backend probe allocation. Native tests cover observation, match, and auto-to-JLA failures. |
| The API12 final subtraction gate is effective. | accepted as scoped positive evidence | Exact and JLA still form `corrected=plugin-correction`, test it for finiteness, and withhold `NONFINITE_CORRECTED_TARGET` before posting. |
| Sampling inference, uniform asymptotics, restricted data, licensing, and package unification were outside the review. | retained limitation | API13 remains an internal finite point-estimator candidate with no `e(V)`, public license, restricted-data qualification, or unified wrapper. |

## Status decision

The response is a valid `false` review of API12 and is
`ai_reviewed_once` evidence only. Its critical and major findings are repaired
in API13. It does not close KB5 because the reviewed source changed; API13
requires two fresh independent completed reviews with no access to this
response.
