# External proof-review request: KSS API 11 quotient and canonical-control closure audit K

You are an adversarial mathematical reviewer. Reconstruct and challenge the supplied proof step. Do not assume the author's conclusion. Distinguish an exposition omission from a genuine missing argument.

## Milestone and obligation

- Milestone: KB5
- Obligation: KB5-FINAL-PRODUCTION-CONTRACT-K
- Critical: True

## Question

Does the supplied API 11 Stata/Mata implementation satisfy the governing finite point-estimator contract on every accepted exact and improved-JLA path, including the explicit-control rank contract and fixed-seed invariance under firm-ID relabeling and invertible control-basis transformation?

## Assumptions you may use

- The review is limited to finite fixed-design point estimation and numerical acceptance. It makes no sampling-inference, exact finite-R unbiasedness, uniform-asymptotic, manuscript, restricted-data, licensing, or public-release claim.
- Positive integer frequency weights are literal physical copies. Accepted fixed-seed JLA executions must use the same conceptual sign stream after harmless row sorting, ID relabeling, and regrouping identical physical copies.
- An invertible map Z to ZT defines the same control span. Accepted outputs must agree within the registered numerical tolerance; a typed conservative failure is allowed when the control span cannot be canonically certified at the rank margin.
- Both nuisance conventions require an identified full worker--firm--control fit. Explicit zero or collinear numeric controls must not be silently removed. Only factor terms carrying Stata omitted metadata may be dropped.
- The public PCG tolerance is bounded above by 1e-4. This cap is supplementary: independently check that the full-firm quotient solve is permutation equivariant and that the canonical control basis makes adaptive inverse actions basis invariant.
- Conservative typed withholding is permitted. False acceptance, generalized-inverse or ridge repair, silent deletion-unit or estimand changes, partial targets, stale-runtime execution, and tolerance relaxation are not.
- Passing tests is falsification evidence only. Reconstruct production code independently, including the registered six-row K(2,3), determinant-four control map, explicit-zero, six-cycle, and literal-copy counterexamples, without using any earlier model review.

## Review constraints

- Use only the supplied packet unless a standard theorem is explicitly identified and stated.
- Check quantifiers, conditioning, uniformity, exceptional events, normalization, and rate arithmetic.
- Search for counterexamples and hidden dependence.
- Do not certify the entire paper from a local argument.
- Cite packet file paths and line ranges for every criticism.

## Required output

- A verdict: valid, valid with repairs, unresolved, or false; state whether any critical objection remains before KB5 closure.
- Authenticate every supplied body against the manifest, then reconstruct every accepted public path through preprocessing, graph selection, full fit, rank gates, deletion adjustment, four target contractions, and posted matrices, with packet line citations.
- Re-run the six-row K(2,3) probes(2) attack under all displayed base firms and explain whether the full-firm zero-sum quotient PCG and post-solve grounding remove encoded-ID dependence for scalar and batched right-hand sides.
- Prove or disprove that the whitening plus invariant row-anchor construction maps Z and ZT to the same numerical control basis on accepted inputs. Re-run the determinant-four map at probes(2), both nuisance conventions, and the maximum allowed tolerance.
- Re-run positive/negative-zero, zero-plus-valid-control, frequency-two, exact/JLA, joint/fixed-offset, observation/match, and auto-dispatch rank attacks. Confirm actual omitted factor bases remain the only preprocessing removal.
- Recheck literal-copy sufficient statistics, coefficient-one fourth moment, constrained P/M ratios, block-control projection, +B and -V signs, target normalization, total accounting, parameter metadata, failure labels, and physical-copy limit.
- Separate critical estimator defects from conservative withholding, optional numerical hardening, documentation issues, and claims outside scope.
