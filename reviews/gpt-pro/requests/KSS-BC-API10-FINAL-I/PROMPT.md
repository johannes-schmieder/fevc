# External proof-review request: KSS API 10 invariant-stream and full-rank closure audit I

You are an adversarial mathematical reviewer. Reconstruct and challenge the supplied proof step. Do not assume the author's conclusion. Distinguish an exposition omission from a genuine missing argument.

## Milestone and obligation

- Milestone: KB5
- Obligation: KB5-FINAL-PRODUCTION-CONTRACT-I
- Critical: True

## Question

Does the supplied API 10 Stata/Mata implementation satisfy the governing finite point-estimator contract on every accepted exact and improved-JLA path? In particular, do its conceptual-copy probe ordering and tie gate make fixed-seed output invariant to literal frequency regrouping, row order, ID relabeling, and invertible control reparameterization, and does every JLA control fit, including nuisance(fixedoffset), fail closed when the full worker--firm--control design is unidentified?

## Assumptions you may use

- The review is limited to finite fixed-design point estimation and numerical acceptance. It makes no sampling-inference, exact finite-R unbiasedness, uniform-asymptotic, manuscript, restricted-data, licensing, or public-release claim.
- Positive integer frequency weights are literal physical copies. For a fixed seed and probe count, accepted observation-JLA output must agree after splitting or regrouping identical stored rows when per-copy outcome, target mass, controls, coordinate, and deletion semantics are unchanged.
- Accepted fixed-seed output must be invariant to row order, pure worker/firm ID relabeling, and invertible reparameterization of the same control span. A typed conservative failure is allowed where no representation-free pathwise copy order can be certified.
- Both nuisance conventions require an identified full worker--firm--control fit. The fixed-offset convention then holds its full-sample fitted control index fixed during deletion; it may conservatively apply a stronger deletion-rank certificate.
- Conservative typed withholding is permitted. False acceptance, generalized-inverse or ridge repair, silent deletion-unit or estimand changes, partial targets, stale-runtime execution, and tolerance relaxation are not.
- Passing tests is falsification evidence only. Reconstruct and challenge production code independently, including the two API9 counterexample classes described by the registered regression fixtures, without relying on any prior model review.

## Review constraints

- Use only the supplied packet unless a standard theorem is explicitly identified and stated.
- Check quantifiers, conditioning, uniformity, exceptional events, normalization, and rate arithmetic.
- Search for counterexamples and hidden dependence.
- Do not certify the entire paper from a local argument.
- Cite packet file paths and line ranges for every criticism.

## Required output

- A verdict: valid, valid with repairs, unresolved, or false; state whether any critical objection remains before KB5 closure.
- Reconstruct every accepted path from public inputs through sample and graph selection, full fit, rank gates, deletion adjustment, four target contractions, and posted matrices, with packet line citations.
- Re-run algebraically the six-copy probes(2) frequency-splitting attack, ID relabeling, control-basis transformation, and tied-key cases; decide whether accepted fixed-seed results are now pathwise representation invariant.
- Re-run the six-cycle fixed-offset singular-control attacks at tiny scale/default tolerance and ordinary scale/loose tolerance; decide whether any approximate solve can still manufacture an accepted nuisance dimension.
- Verify the copywise JLA sufficient statistics, coefficient-one fourth moment, constrained ratios, block-control projection, +B and -V inverse signs, target normalization, and total accounting identity.
- Separate critical estimator defects from conservative withholding, optional numerical hardening, metadata or documentation issues, and claims outside scope.
