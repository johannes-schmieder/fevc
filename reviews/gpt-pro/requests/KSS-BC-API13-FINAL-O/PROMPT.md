# External proof-review request: KSS API 13 semantic-order and numerical-certificate closure audit O

You are an adversarial mathematical reviewer. Reconstruct and challenge the supplied proof step. Do not assume the author's conclusion. Distinguish an exposition omission from a genuine missing argument.

## Milestone and obligation

- Milestone: KB5
- Obligation: KB5-FINAL-PRODUCTION-CONTRACT-O
- Critical: True

## Question

Does the supplied API 13 Stata/Mata implementation satisfy the governing finite point-estimator contract on every accepted exact and improved-JLA path, in particular after the common semantic-order, exact-frequency, dimensioned control-certificate, and all-JLA allocation repairs?

## Assumptions you may use

- The review is limited to finite fixed-design point estimation and numerical acceptance. It makes no sampling-inference, exact finite-R unbiasedness, uniform-asymptotic, manuscript, restricted-data, licensing, or public-release claim.
- Positive integer frequency weights are literal physical copies. Accepted fixed-seed JLA executions must use the same conceptual sign stream after harmless row sorting, ID relabeling, and regrouping identical physical copies.
- API13 withholds before graph ranking when the exact literal frequency total exceeds 2^53. Check that the incremental gate is exact and that every accepted component mass, deletion count, and N_physical value remains an exactly represented integer.
- An invertible map Z to ZT defines the same control span. Accepted outputs must agree within the registered numerical tolerance; a typed conservative failure is allowed when the canonical anchor cannot be certified at the rank margin.
- Audit the API13 canonical-control certificate itself. It caps the control count at 32; includes dimensioned gamma terms for Gram, Cholesky, and products; converts maximum-column inverse residuals through sqrt(k) and a positive reciprocal-conditioning denominator; includes score/cutoff, projector, anchor, and span checks; and propagates E/(r-E) through full and deletion margins with a 1e-8 ceiling. Do not assume these formulas establish the claimed accepted-path invariance.
- Both nuisance conventions require an identified full worker--firm--control fit. Explicit zero or collinear numeric controls must not be silently removed. Only factor terms carrying Stata omitted metadata may be dropped.
- Finite plug-in and correction rows are insufficient if their difference is nonfinite. No success path may post a missing, infinite, or partial corrected target.
- The public PCG tolerance is bounded above by 1e-4. Independently check the full-firm quotient solve, scalar and batched permutation equivariance, canonical-control inverse actions, and recomputed residual gates.
- Conservative typed withholding is permitted. False acceptance, generalized-inverse or ridge repair, silent deletion-unit or estimand changes, partial targets, stale-runtime execution, and tolerance relaxation are not.
- Passing tests is falsification evidence only. Reconstruct production code independently and do not use any earlier model review or chat.

## Review constraints

- Use only the supplied packet unless a standard theorem is explicitly identified and stated.
- Check quantifiers, conditioning, uniformity, exceptional events, normalization, and rate arithmetic.
- Search for counterexamples and hidden dependence.
- Do not certify the entire paper from a local argument.
- Cite packet file paths and line ranges for every criticism.

## Required output

- A verdict: valid, valid with repairs, unresolved, or false; state whether any critical objection remains before KB5 closure.
- Authenticate every supplied body against the manifest, then reconstruct every accepted public path through preprocessing, graph selection, full fit, rank gates, deletion adjustment, four target contractions, final finite subtraction, and posted matrices, with packet line citations.
- Prove or disprove that the API13 common semantic ordering and dimensioned canonical-control certificate are sufficient for accepted Z and ZT inputs to produce the same numerical canonical basis and downstream result at the registered tolerance. Audit every gamma term, norm conversion, perturbation denominator, pivot, final span check, and full/deletion propagation gate; search adversarially over ties, score gaps, dimensions, conditioning, scaling, rotations, shears, and later pivots.
- Re-run the four-row cutoff, 14-row determinant-one, eight-row determinant-four, and nine-row Q versus -Q fixtures across their registered exact/JLA/auto, deletion, nuisance, relabeling, batch, probe, seed, and tolerance cases. Distinguish valid typed withholding from basis-dependent acceptance.
- Construct finite opposite-signed plug-in and correction rows whose subtraction overflows, trace both exact and JLA completion paths, and confirm NONFINITE_CORRECTED_TARGET occurs before any e() result is posted.
- Re-run the six-row K(2,3) firm-relabeling attack, determinant-four map, positive/negative-zero, zero-plus-valid-control, frequency-two, six-cycle, literal-copy, and automatic-dispatch attacks.
- Recheck the two-K(2,2) L=2^52 component-mass attack, exact-total boundary, all-JLA physical-copy limit, literal-copy sufficient statistics, coefficient-one fourth moment, constrained P/M ratios, block-control projection, +B and -V signs, target normalization, total accounting, parameter metadata, and failure labels.
- Separate critical estimator defects from conservative withholding, optional numerical hardening, documentation issues, and claims outside scope.
