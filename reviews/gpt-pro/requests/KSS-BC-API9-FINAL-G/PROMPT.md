# External proof-review request: KSS API 9 literal-copy, graph, and numerical closure audit G

You are an adversarial mathematical reviewer. Reconstruct and challenge the supplied proof step. Do not assume the author's conclusion. Distinguish an exposition omission from a genuine missing argument.

## Milestone and obligation

- Milestone: KB5
- Obligation: KB5-FINAL-PRODUCTION-CONTRACT-G
- Critical: True

## Question

Does the supplied API 9 Stata/Mata implementation satisfy the governing finite point-estimator specification for exact and improved-JLA KSS correction, including literal coordinatewise frequency semantics at finite probe counts, ID-invariant fail-closed graph selection, general controls, all four targets, and the registered numerical gates? Identify any accepted production path that can return a point estimate outside that contract.

## Assumptions you may use

- The review is limited to finite fixed-design point estimation and the unconditional random-probe law; it makes no econometric-inference, exact finite-R unbiasedness, uniform-asymptotic, or manuscript claim.
- Positive integer frequency weights are literal physical copies. Under observation deletion the same finite-R algorithm must be applied coordinatewise to every copy; nonlinear ratios may not be pooled before copywise adjustment.
- Conservative typed withholding is allowed. Silent deletion-unit changes, component changes, ridges, tolerance relaxation, partial targets, and stale-runtime execution are not allowed.
- Match mode uses the documented mover-only fit and target. Ties in the registered largest-component ranking have no authorized winner and must withhold.
- Low-dimensional controls may vary within a declared match. Joint and fixed-offset modes have the meanings in the estimator contract.
- Passing executable tests is falsification evidence and does not prove an asymptotic theorem.

## Review constraints

- Use only the supplied packet unless a standard theorem is explicitly identified and stated.
- Check quantifiers, conditioning, uniformity, exceptional events, normalization, and rate arithmetic.
- Search for counterexamples and hidden dependence.
- Do not certify the entire paper from a local argument.
- Cite packet file paths and line ranges for every criticism.

## Required output

- A verdict: valid, valid with repairs, unresolved, or false; state whether any critical objection remains before KB5 closure.
- Trace the public command into every accepted exact and JLA formula; cite packet paths and line ranges for each objection.
- Re-derive the API 9 per-copy sufficient statistics, constrained ratios, finite-bias and variance terms, final within-row aggregation, target scaling, and four-target accounting. Decide whether collapsed and literally expanded executions implement the same finite-R random estimator.
- Re-derive the general match-control projection and inverse adjustment, and challenge both dense and JLA deletion-rank gates with ill-conditioned or singular finite designs.
- Audit component-tie detection, ID relabeling, row ordering, frequency splitting, semantic runtime identity, and all fail-closed caller routes.
- Separate critical point-estimator defects from conservative withholding, metadata hardening, exposition suggestions, and claims explicitly outside scope.
