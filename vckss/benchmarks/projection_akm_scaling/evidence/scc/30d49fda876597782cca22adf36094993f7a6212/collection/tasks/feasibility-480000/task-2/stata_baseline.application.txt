. do /projectnb/welfgr/vckss/runs/20260830T2006Z-projection-akm-censor-30d49fd/
> source/vckss/benchmarks/projection_akm_scaling/stata_baseline.do 16 4 

. version 18.0

. clear all

. set more off

. args cores_arg processors_arg

. local cores = real("`cores_arg'")

. local processors = real("`processors_arg'")

. assert inlist(`cores',4,16) & `processors'==min(4,`cores')

. set processors `processors'
    The maximum number of processors or cores being used is 4.  It can be set
    to any number between 1 and 4.

. di as result "VCKSS PROJECTION AKM STATA BASELINE PASS cores=`cores' processo
> rs=`processors'"
VCKSS PROJECTION AKM STATA BASELINE PASS cores=16 processors=4

. exit 0

end of do-file



. 