# Rust/Mata parity matrix

Generated from `rust_mata_parity.json`; do not edit this file by hand.
Historical receipts remain source-bound and do not change these current statuses.

Target: `0.5.0-alpha.1`. Updated: 2026-08-31.

| Surface | Mata | Rust | Alpha gate | Current evidence or gap |
|---|---|---|---:|---|
| Exact, movers | qualified | qualified | yes | match/observation deletion; joint/fixed-offset controls; weights, targets, deletion IDs |
| JLA, compressed | qualified | qualified | yes | no-control match specialization with Counter-V1 receipts |
| JLA, generic | qualified | qualified | yes | controls, both deletion modes, both nuisance modes, diagonal/CMG planning |
| algorithm(auto), exact selection | qualified | qualified | yes | public V3/V4/V7 exact-family reconciliation and zero-RNG posting |
| algorithm(auto), JLA selection | qualified | qualified | yes | public V3/V4/V7 generic and compressed selection qualified on macOS at daca3e2 |
| probeorder() | qualified | qualified | yes | semantic tie-break key, V3 preparation ABI, exact memory admission, public generic/compressed JLA, permutation/batch invariance, and clean install qualified on macOS at 3bc6a89 |
| stayers(both), exact and generic-JLA mixed deletion | qualified | implemented | yes | exact and generic-JLA combined routes passed the complete local macOS arm64/Rosetta thin, universal, and clean-install matrix against source manifest 3eaab178e400422e2418721d36f7d10141f56f3269b43f1b892a2f21b5cd913d; checkout-clean release qualification remains required after commit |
| Rust-preferred backend(auto) default | qualified | qualified | yes | effective-option admission, rng(auto), strict consent, preflight-only fallback, and native success qualified on macOS at daca3e2 |
| Caller data, sample, sort, and RNG restoration | qualified | qualified | yes | success, typed failure, corrupt receipt, and UserBreak coverage |
| Exact observation-deletion component inference | qualified | deferred | no | opt-in high-rank covariance and q=1 weak-identification intervals; unit frequency weights; focused dense, state-restoration, critical-value, and typed-failure tests |
| Fixed-effect projection inference | qualified | qualified | no | exact Mata and sparse Rust generic-JLA routes; explicit diagonal or forced generic CMG; positive integer frequency weights match literal expansion; maintained-MATLAB weighted oracle; automatic constant; frequency or target mass; streamed covariance and complete residual/PSD/memory gates; automatic projection routing withheld |
| macOS arm64 and Rosetta qualification | qualified | qualified | yes | thin and universal source-local plugin-build profile at c199bf0, including exact stayer hybrid |
| Linux x86-64 / SCC qualification | qualified | qualified | yes | Stata MP 19 full suite and isolated clean installation on SGE job 7305794; exact source 86e0711, candidate and scheduler receipts under M4-LINUX-SCC |
| Windows qualification | implemented | deferred | no | not an alpha release gate and no Windows binary will be shipped |

An alpha-required row is complete only when Rust is `qualified` on every
claimed platform and the exact-source receipt is linked from the active plan.
`implemented` means source exists but the release qualification is incomplete.
