# Fixed-offset match release-candidate checkpoint

## Current decision

Not yet a release candidate. The two match scientific prerequisites are
available, but the approved fresh observation prerequisite has one failed
frozen gate. Public match integration pauses before any ABI/ado/help promotion.
No scientific cutoff or target exclusion has changed. No additional campaign,
joint-control correction, push, tag, release or binary distribution is started.

| Gate | State | Evidence or remaining work |
| --- | --- | --- |
| Full independent fixed-offset match q0 | PASS | 140,000 attempts, all original gates and 72 accounting records; [result](RC_MATCH_Q0_CONFIRMATION_2026-09-05.md) |
| Repaired eligible match q1 | PASS, reused with limits | Accepted 140,000-attempt confirmation and [source compatibility review](RC_INFERENCE_SOURCE_COMPATIBILITY_2026-09-05.md) |
| Corrected observation q0/q1 under original V5 gates | FAIL | One leverage-only firm SE ratio 1.101204 exceeds 1.10; all 200,000 attempts audited; [result](RC_OBSERVATION_CONFIRMATION_2026-09-05.md) |
| Current source engineering checks | PASS | 684 Python tests; Rust format, strict Clippy and all workspace targets; CMG assembler |
| Explicit public match route and diagnostic transport | NOT STARTED | Scientific prerequisite/owner decision first |
| Affected exact-source native, Stata and clean installation | NOT STARTED for new route | Existing unchanged-boundary evidence is not qualification of a future match interface |
| Exact artifact, notices, source correspondence and human release review | OWNER GATE | Separate permission required; no artifact is staged for release |

## Next bounded decision

The narrow observation miss remains a FAIL. Recommended next work, only after
owner direction, is to use the already completed outputs to assess Monte
Carlo uncertainty in the failed SE ratio and compare the leverage-only row
with the primary structured-common specification. This is not a new coverage
campaign, estimator search, or license to pass the old result retrospectively.
An owner decision about a narrower release scope would be recorded separately
and prospectively, leaving this failure and every historical receipt intact.

The result does not undermine the separately passed match q0/q1 fixed-offset
evidence. Joint-control match inference, another controls experiment, a second
stage correction, new covariance estimators, automatic q selection and q>1
remain outside this milestone.

## Implementation after the scientific/release-scope decision

1. Expose only explicit `deletion(match) nuisance(fixedoffset) stayers(movers)`
   component inference with Rust generic JLA, Counter-V1, an explicit
   `structured_common` or separately authorized `structured_leverage` model,
   an explicit diagonal/CMG solver, and `inference(highrank)` (q0) or
   `inference(q1)`. Preserve the existing observation tuple and reject
   unsupported combinations before RNG/native work. Do not change joint
   nuisance or combined mover/stayer point defaults.
2. Attach the existing grouped kernel through the native session; add
   versioned transport/reconciliation for match units, independent/effective
   match counts, mass concentration, match leverage, maker denominator and
   omitted nuisance uncertainty. Preserve current target-specific q1
   withholdings and whole-request structural failures. No covariance or
   estimator rewrite belongs in this boundary change.
3. Test declared match IDs separately from worker--firm coordinates, positive
   frequency mass without treating copies as independent clusters, allowed
   target weights, point invariance, fixed folds, batch/order invariance,
   lifecycle/RNG restoration, explicit unsupported cases, and version/capacity
   guards. Keep physical-block/dense oracles independent of production code.
4. Update help, `e()` returns, display and `estat diagnostics` coherently.
   Required label: **Fixed-offset approximate match inference, ignoring
   nuisance-control estimation uncertainty.** Explain independence across
   matches, arbitrary within-match dependence through aggregate variance,
   the named structured variance assumption, q-specific concentration
   conditions, target-specific availability and severe misspecification.
   Do not claim that estimating only a few controls guarantees negligible
   uncertainty, or call this unrestricted KSS inference.
5. Run focused Rust/C/Stata tests, the minimum Python/CMG gates, integrated
   package checks and clean installation. Commit a clean exact source and
   qualify the affected macOS arm64/Rosetta plugin-build profile. Record
   exact artifact identities and a scientific source-compatibility review;
   do not rerun large scientific arrays just because the SHA changes.
6. Prepare an owner-reviewable RC summary listing supported tuples, known
   limitations, deferred platforms/performance claims, source and native
   artifacts, reproducible installation, licenses/notices and remaining
   human approvals. Ask separately before publishing, tagging or distributing
   any binary. Windows and a broad new scaling campaign are not automatically
   added to this checkpoint.

## Evidence preservation

Both new campaigns have verified ignored archives in `.local/diagnostics/`.
They retain exact source bundles/binaries, raw per-task and aggregate output,
manifests, task/build receipts, final logs, complete scheduler accounting,
independent local audits and prerequisite evidence. Their SHA-256 values and
full failure inventories are in the linked JSON result records. The failed
observation aggregate's deliberate exit 1 is distinguished from scheduler
failure. No rerun or outcome-based exclusion was performed.
