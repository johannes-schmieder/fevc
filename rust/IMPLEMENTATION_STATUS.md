# Rust backend implementation status

Status snapshot: 2026-08-21 on `codex/rust-backend-finish` at
`f2fa740ea4d65c84858484d16a1e042129658675`.

PR #2 was merged with preserved history at
`f70a1457abd6c6fab826c3334c72bdea5b0cef5b`; its former head
`e6e84ea71f53b95f82554a34b1749ff1b1afa5df` is an ancestor of that merge.
The current core repair is `f2fa740`. Draft PR #3 is open at that commit.

The core-only test suite passes all 53 tests, plus zero doctests, with both
Rust 1.81.0 (`aarch64-apple-darwin`) and stable 1.97.1
(`aarch64-apple-darwin`). These were local runs on 2026-08-21; a durable CI
receipt has not yet been recorded. The full workspace and standalone crate
remain red. Current blockers are the missing `vckss_core::engine` module,
`ErrorCode::InvalidId` references where the enum defines
`InvalidIdentifier`, production module omissions, the stale standalone
lockfile, and absent Stata SDK sources expected by the standalone build.
The non-mutating Rust 1.81 `cargo fmt --check` gate is also red: it reports
broad formatting diffs across pre-existing workspace files and the current
core edits. No formatting changes or normalization commit have been made.

Every public `supports_*` capability flag is `false`. `VCKSS-COUNTER-V1` is a
Rust-native counter contract distinct from the registered Mata/Stata `mt64s`
contracts. Nothing in this status establishes Rust--Mata parity, production
qualification, cross-platform Stata support, scale qualification, or release
readiness.

| Subsystem | Implementation status | Production wiring status | Unit-test status | Mata parity status | Stata integration status | Cross-platform status | Scale status | Known limitations |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Capability and contract surface | Versioned ABI, numerical, receipt, CMG, and counter-RNG constants exist. Internal `core_*_ready` fields are separate from public support. | Capability JSON is exported by prototype and standalone libraries. | 2 core library tests pass within the 53-test runs. | Not tested. | Developer wrapper can request capabilities, version, and self-test only after a plugin builds. | No passing matrix evidence. | Not applicable. | All seven public support flags (`exact`, `jla`, `match_deletion`, `observation_deletion`, `controls`, `diagonal`, `cmg`) remain `false`, correctly preventing a support claim. |
| Input types and resource checks | Checked options, finite outcomes/controls, positive frequency, nonnegative target mass, exact-binary64 physical total, dimensions, and a simple memory budget exist. | Preparation FFI converts six numeric columns, but the production plugin build is red. | 4 type tests pass within the core runs. | Not tested. | Preparation-only path drafted. | No passing matrix evidence. | No allocation-failure or target-scale admission evidence. | The C ABI accepts no controls or numerical options; it classifies frequency failures as identifier failures and refers to the nonexistent `InvalidId` variant. |
| Canonicalization and compression | Deterministic dense maps, cell/deletion/target indices, sufficient statistics, and topology checksum exist on the `u32` implementation path. | Used by prototype preparation sessions. | 3 compression tests pass within the core runs. | No Rust--Mata differential fixture. | No retained-mask export in the compiled standalone path. | No passing matrix evidence. | Small fixtures only. | No qualified `u64` cardinality path, full relabeling matrix, allocation-failure injection, or scale evidence. |
| Match-deletion graph selection | Largest-component, mover, degree, worker-articulation, deletion-bridge fixed point and final checks are implemented. | Called during prototype preparation. | 5 graph tests pass within the core runs. | Not established; no exact retained-row/map comparison with current Mata. | The path-included retained session holds a marked-row mask, but it is omitted from the standalone module graph and C dispatcher. | No passing matrix evidence. | Small finite graphs only. | Observation deletion is not implemented; graph-shape and fixed-point cascade coverage is incomplete. |
| Two-way operator, exact solve, and full residual | Full zero-sum firm quotient, Schur action, worker reconstruction, dense exact solve, and complete worker-plus-firm residual certification exist for no-control problems. The `f2fa740` repair removes numeric-last-firm dependence. | Callable from core routing, not from a working public engine. | 4 operator/exact tests pass within the core runs. | No source-bound or differential Mata qualification. | No numerical result reaches Stata. | No passing matrix evidence. | Exact path is intentionally small; only small fixtures tested. | Controls and nuisance modes are unsupported; no end-to-end KSS exact route. |
| Scalar/batched PCG and solver routing | Diagonal scalar PCG, batched scalar PCG, per-RHS receipts, residual replacement, complete-residual checks, exact/diagonal/CMG routing, and setup-only auto fallback exist. `f2fa740` projects all quotient vectors and uses `F-1` for route limits. | Core-only; the missing engine prevents public use. | 14 Krylov, batch, and routing tests pass within the core runs. | No Mata comparison; no comprehensive failure-injection matrix. | Not exposed by the standalone C header or dispatcher. | No passing matrix evidence. | Small synthetic fixtures only. | No interruption/cancellation path, broad stagnation/drift coverage, or target-scale repeated-RHS evidence. |
| CMG hybrid graph and hierarchy | Source-informed hybrid graph, deterministic hierarchy, bounded dense terminal, reusable symmetric V-cycle, receipts, and CMG preconditioner exist. `f2fa740` adds full-quotient and relabeling repairs. | Available to the core router only. | 8 hybrid/hierarchy tests pass within the core runs, including density-four and relabeling fixtures. | Not established. Source provenance and Rust self-consistency are not Mata parity. | No production solve/export route. | No passing matrix evidence. | Tested on small density-four and complete-bipartite fixtures only. | No API-7 Mata differential receipts, neighboring-density suite, measured allocation comparison, large hierarchy reuse, or scale timings. |
| Counter RNG | `VCKSS-COUNTER-V1` implements Philox4x64-10, frozen vectors, domain separation, Rademacher atoms, and batch-addressed generation. | Available as a core utility; the missing engine does not consume it end to end. | 5 RNG tests pass within the core runs. | Deliberately distinct from Mata/Stata `mt64s`; fixed-seed equality must not be claimed. | Does not mutate Stata RNG because it is not yet on a Stata estimator route. | Frozen vectors have not been recorded from the CI platform matrix. | No large-frequency or target-scale throughput evidence. | No separately named Stata-compatible contract; row/thread invariance is not qualified end to end. |
| JLA semantic plan and component helpers | No-control deletion and target semantic plans, fitted/residual values, plugin components, and accounting checks exist. | Used by preparation and expected by the missing engine. | 4 JLA tests pass within the core runs. | Not established. | No corrections, corrected estimates, MCSEs, or receipts reach Stata. | No passing matrix evidence. | Small fixtures only. | Leverage and target probe pipelines, KSS corrections, finite-probe adjustment, MCSEs, retries, and per-RHS policy are not implemented in a compiled engine. |
| End-to-end numerical engine | Expected `run_jla_no_controls`, options, and result types are referenced by tests and prototype FFI. | Not wired: `engine.rs` and `pub mod engine` are absent. | Engine tests do not compile. | None. | None. | None. | None. | This is the primary blocker to preparation-through-corrected-estimate ownership. |
| Native context, retained mask, and numerical FFI | Generation-checked context lifecycle, panic containment, preparation session, retained-session prototype, and numerical-FFI draft exist. | `session_retained.rs` and `ffi_engine.rs` are omitted from both production module graphs; several integration tests use `#[path]` inclusions instead. | Context/session tests exist, but the full plugin target set is red because of missing engine and `InvalidId`. | None. | Compiled standalone surface is preparation-only. | No passing matrix evidence. | No stress, leak, sanitizer, or Miri evidence. | Two divergent registries remain; numerical options are default-only; result and detailed receipt ABI is incomplete and unqualified. |
| Standalone plugin and C shim | Rust exports capabilities/self-test and preparation ABI. C header and dispatcher support capabilities, version, self-test, prepare, snapshot, release, and clear. | Red: standalone lockfile is stale, `InvalidId` is unresolved, engine modules are omitted, and `rust/include/stplugin.c` plus `stplugin.h` are absent. | Standalone tests have not passed at `f2fa740`. | None. | No plugin load test. | Workflows describe Linux, Windows, and macOS builds, but no current passing artifacts exist. | No ingestion or native-memory measurements. | No retained-mask, solve, result, detailed-receipt, or interruption commands; ABI layout has not been checked on target platforms. |
| Stata command routing and state protection | `varcomp_kss_rust.ado` is a developer lifecycle wrapper. | The public `varcomp_kss` command has no `backend(auto)`, `backend(mata)`, or `backend(rust)` route. | No Rust Stata integration test has run. | None. | No licensed Stata evidence for load, retained `e(sample)`, estimates, caller data/sort/RNG restoration, errors, or interruption. | No platform Stata evidence. | None. | Production estimator integration, `e()` results, fallback receipts, and state protection are absent. |
| Deterministic parallelism and run receipts | Fixed partitions, ordered joins, deterministic sums, timer/memory receipt types, and selected solver receipts exist. | Not assembled into one long-lived production command runtime or complete export surface. | 4 parallel/receipt tests pass within the core runs. | Not tested. | Not exported as production receipts. | No passing matrix evidence. | No throughput, scaling, RSS, or modeled-versus-measured evidence. | Thread-count invariance of the complete estimator is untested; many requested receipt fields are absent. |
| CI, qualification, and release | Two workflows declare Rust 1.81/stable and three operating systems; the standalone workflow declares platform artifacts. | Draft PR #3 had no reported checks at this snapshot. The main workspace workflow still runs formatting without `--check`. | Core-only local evidence is green; format, workspace, and standalone gates are red. | No parity claim. | No Stata qualification claim. | No qualified artifacts or universal-binary inspection receipt. | No ingestion, medium, target-scale, or external-memory evidence. | No sanitizer/fuzz evidence, SBOM, validation report, benchmark report, checksummed release set, or final human mathematical/license/provenance review. Public release remains disabled. |

## Current executable gates

The two core commands below passed locally on 2026-08-21:

```bash
RUSTC=/Users/johannes/.rustup/toolchains/1.81.0-aarch64-apple-darwin/bin/rustc RUSTDOC=/Users/johannes/.rustup/toolchains/1.81.0-aarch64-apple-darwin/bin/rustdoc /Users/johannes/.rustup/toolchains/1.81.0-aarch64-apple-darwin/bin/cargo test --manifest-path rust/Cargo.toml -p vckss-core
RUSTC=/Users/johannes/.rustup/toolchains/stable-aarch64-apple-darwin/bin/rustc RUSTDOC=/Users/johannes/.rustup/toolchains/stable-aarch64-apple-darwin/bin/rustdoc /Users/johannes/.rustup/toolchains/stable-aarch64-apple-darwin/bin/cargo test --manifest-path rust/Cargo.toml -p vckss-core
```

Each reported 53 passed, 0 failed, followed by 0 doctests. The complete gates
remain the commands in `TEST_PLAN.md`; they must turn green before any support,
parity, platform, scale, or release claim changes.
