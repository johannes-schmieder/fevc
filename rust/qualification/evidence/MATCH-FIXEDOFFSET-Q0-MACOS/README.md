# Fixed-offset match q=0 macOS qualification

The exact-source directory under this tree preserves the source-local native
qualification for the internal fixed-offset collapsed-match `q=0` foundation.
The qualifier covers Rust/C/ABI checks, thin arm64 and x86-64 candidates, the
universal plugin, Rosetta, licensed Stata, lifecycle, routing, and clean
installation. The grouped attachment itself is deliberately absent from the
plugin ABI and Stata surface; its behavior is covered by the source-bound Rust
integration and independent-oracle tests.

Candidate hashes are recorded in the text receipt. No plugin binary or raw
Stata log is committed. This is not grouped-inference coverage evidence, a
public match-inference capability, native-Intel/Linux/Windows qualification,
a scale claim, a release, or binary distribution.
