# Structured observation-inference macOS qualification

The exact-source directory under this tree preserves the private native
qualification of the supported explicit structured observation-deletion
component-inference boundary. It covers thin arm64 and x86-64 candidates, the
universal plugin, Rosetta, licensed Stata, clean installation, Rust/C/ABI
checks, lifecycle, routing, versioned result reconciliation, and structured
`q=0`/eligible one-mode `q=1` execution.

Candidate hashes are recorded in the text receipt. No plugin binary or raw
Stata log is committed. This is not a Windows, native-Intel, public-release,
or binary-distribution claim.
