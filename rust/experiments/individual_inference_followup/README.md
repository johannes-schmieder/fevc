# Bounded default-setting follow-up

This is the separately registered response to the failed 200-probe development
run, not a repeat or confirmation. See
`fevc/docs/individual_inference_followup_v1.json`.

`run.py` binds the old source bundle/task receipts, builds a disposable adapter
with one changed option (match q0 spectral iterations 128 -> 512), then freezes
the selection and executable hashes before paired replays. It uses all 15
match-q0 cells at existing replications 0, 133, 266 and 399, plus four unchanged
q1 identity checks: 64 pairs, 128 calls, no new outcome draws. Old baseline
calls must reproduce their saved records. No production Rust is patched.

`numerics.py` reads all 400 saved draws in each of the two flagged q1 cells.
Its quartic ellipse extrema and deterministic transformed Gauss-Legendre
critical integral are independent of production's grid and simulation. The
normal-radius and opposite-parity-half covariance arms are explanatory
counterfactuals, not valid replacement estimators or qualification evidence.
All four targets are retained, with the covariance target marked nonprimary.

Run the 21 focused tests with the repository Python. Run outputs must be new;
the completed local evidence is retained under the ignored directory
`.local/diagnostics/individual-inference-followup-20260906/`.
Do not edit the original development receipts or rerun them with these settings.
Future confirmation requires its own source-bound manifest and owner decision.
